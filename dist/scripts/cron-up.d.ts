#!/usr/bin/env node --experimental-strip-types
/**
 * Adaptive cron frequency adjuster — increase frequency (shorter interval).
 *
 * Usage: ocx cron up <cron_id> [--step N]
 *
 * Safety: Only adjusts crons whose name contains [ADAPTIVE].
 * Schedule kind must be "every". anchorMs is never changed.
 */
export {};
//# sourceMappingURL=cron-up.d.ts.map